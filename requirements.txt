fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
sse-starlette==2.1.3
yfinance>=0.2.40

# v2 additions
pandas-market-calendars>=4.4.0   # NYSE open/holiday gate (graceful fallback if absent)
# anthropic>=0.40.0              # OPTIONAL — only needed if you enable Claude judgment (BOT_LLM=1)
