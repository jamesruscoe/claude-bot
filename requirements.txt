fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic==2.9.2
python-dotenv==1.0.1
sse-starlette==2.1.3
yfinance>=0.2.40

# v2 additions
pandas-market-calendars>=4.4.0   # NYSE open/holiday gate (graceful fallback if absent)
# LLM judgment is optional (BOT_LLM=1). Default provider is Groq (free), which
# uses httpx (already above) — no extra dependency. Only the paid Anthropic
# provider needs an SDK:
# anthropic>=0.40.0              # OPTIONAL — only if BOT_LLM_PROVIDER=anthropic
